import React, { useState } from 'react';

const UsePasswordToggle = (visible) => {
    const [visible, setVisible] = useState(false)
    // const icon = (icon={visible ? <FaEye />})

    return (
        {}
    )
}

export default UsePasswordToggle;